import { RegionList } from './const';
export function getRegionByAdcode(code) {
  var region = Object.values(RegionList).find(function (region) {
    return region.child.indexOf(code) !== -1;
  });
  return region;
}