import { VectorTileLayer } from '@mapbox/vector-tile';
import { Feature } from 'geojson';
import { IParserData, IRasterTileParserCFG } from '../interface';
export declare function osmTileXY2LonLat(x: number, y: number, zoom: number): number[];
export declare type MapboxVectorTile = {
    layers: {
        [_: string]: VectorTileLayer & {
            features: Feature[];
        };
    };
};
export default function mapboxVectorTile(data: string | string[], cfg?: IRasterTileParserCFG): IParserData;
