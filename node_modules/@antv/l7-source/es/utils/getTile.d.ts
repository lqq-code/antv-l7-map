import { Tile, TileLoadParams } from '@antv/l7-utils';
/**
 * 用于获取 raster data 的瓦片，如 tiff、lerc、dem 等
 * @param url
 * @param tileParams
 * @param tile
 * @param rasterParser
 * @returns
 */
export declare const getTileBuffer: (url: string | string[], tileParams: TileLoadParams, tile: Tile, rasterParser: any) => Promise<HTMLImageElement | ImageBitmap>;
export declare const getTileImage: (url: string | string[], tileParams: TileLoadParams, tile: Tile) => Promise<HTMLImageElement | ImageBitmap>;
export declare const defaultFormat: () => {
    rasterData: Uint8Array;
    width: number;
    height: number;
};
